1. 使用dev分支打tag,上传到github,通过github生成release note和release分支
2. 进行打包生成最新代码
3. 删除package.json的private,devDependencies,jest节点
4. 运行publish:npm,如果有报错使用cmd运行npm login重新登录验证,再次运行publish:npm
5. 撤回刚才修改的package.json,修改package.json的版本号到下一个版本
6. cmd运行npm uninstall all更新package-lock.json,然后提交代码即可