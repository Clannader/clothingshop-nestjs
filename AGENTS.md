# Agent 工作规范

## Git 提交规则

1. **在当前分支提交**：修改任何文件前，确保在当前工作分支上进行提交，不得切换到 `master` 分支提交代码。
2. **先提交再推送**：所有改动必须先在当前分支 `git commit`，确认提交成功后才能 `git push`。
3. **禁止自动创建 Pull Request**：不得擅自创建 Pull Request。如需建立 PR，必须先询问用户并获得明确同意后才可操作。
4. **禁止提交 config.ini**：`config/config.ini` 已在 `.gitignore` 中被忽略，禁止将其加入暂存区或提交。该文件包含本地环境配置，不应被跟踪。
5. **提交需用户确认**：执行 `git commit` 前必须先向用户展示待提交的文件清单和提交信息，获得用户明确同意后才能执行提交；用户未确认前禁止擅自 commit。
